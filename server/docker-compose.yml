services:
  mongodb:
    image: mongo:latest
    container_name: finalproject_mongodb
    restart: always
    environment:
      MONGO_INITDB_DATABASE: tasteshare
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
