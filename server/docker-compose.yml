services:
  mongodb:
    image: mongo:latest
    container_name: assignment1_mongo
    restart: always
    environment:
      MONGO_INITDB_DATABASE: testshare
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
